프로젝트 전체에서 공통으로 사용하는 정의를 모아놓는 파일이다.

<30>
HAL_GPIO_WritePin()
HAL_UART_Init()
HAL_Delay()
GPIO_InitTypeDef
UART_HandleTypeDef
등 HAL관련 정의를 전부 사용할 수 있다.

보통 Cubemx는 핀 define을 이곳에 생성한다. 현재 프로젝트는
GPIO 출력 없음
GPIO 입력 없음
LED 없음
버튼 없음
UART의 경우 GPIO일반 핀이 아닌 Peripheral Alternate Function으로 설정되기 때문에 main.h에 별도 매크로를 만들 필요가 없다.